# 기능 명세: <기능명>

> **무엇이 어떻게 동작하는가.** 규칙을 적는다.
> 왜 만드는지는 [`prd.md`](prd.md), 어떤 코드로 만드는지는 [`tech-spec.md`](tech-spec.md).
> 화면 구성은 [`screens.md`](screens.md) 가 갖는다.

---

> 기능마다 아래 항목을 다 채울 필요는 없다. **해당 없는 항목은 지운다.**
> 빈 표를 남기면 읽는 사람이 "여기 뭔가 빠졌나" 하고 멈춘다.

## 기능 목록

| 번호 | 기능 | 한 줄 설명 |
|---|---|---|
| 1 | | |

## 권한

기능마다 반복하지 않고 여기서 한 번 정한다. 기능별 예외만 그 기능 블록에 적는다.

| 사용자 종류 | 기본 권한 |
|---|---|
| | |

## 엔티티 상태 변화

이 기능이 다루는 데이터가 어떤 상태를 거치는지. 상태를 갖는 데이터가 없으면 이 절을 지운다.

```
<상태 A> --(<사건>)--> <상태 B> --(<사건>)--> <상태 C>
```

---

## 1. <기능명>

### 입력

| 항목 | 타입 | 필수 | 제약 |
|---|---|---|---|
| | | | |

### 처리 규칙

<!-- 순서대로. 조건과 분기를 명확히 -->

1.
2.

### 출력

| 상황 | 결과 |
|---|---|
| 성공 | |

### 예외 처리

| 상황 | 처리 | 사용자에게 보이는 것 |
|---|---|---|
| | | |

### 권한 예외

문서 상단의 기본 권한과 다른 경우만 적는다. 같으면 이 절을 지운다.

| 경우 | 허용 |
|---|---|
| | |

---

<!-- 기능마다 위 블록 반복 -->

---

## 기능 간 관계

| 기능 | 선행 조건 | 영향 |
|---|---|---|
| | | |

---

## 데이터 영향

| 엔티티 | 생성 | 수정 | 삭제 |
|---|---|---|---|
| | | | |

새 엔티티가 필요하면 [`../../data-model.md`](../../data-model.md) 에 반영한다.

---

## 경계값

기능별 예외 처리에서 다루지 않은 것만 적는다. 같은 내용을 두 곳에 쓰지 않는다.

| 상황 | 기대 동작 |
|---|---|
| 빈 값 | |
| 최대 길이 초과 | |
| 중복 요청 | |
| 동시 수정 | |

동시 수정을 다룰 때는 [`../../data-model.md`](../../data-model.md) 의 "동시 수정 대비" 결정을 확인한다. 버전 컬럼이 없는 엔티티인데 여기서 동시 수정을 막아야 하면 데이터 모델을 먼저 고쳐야 한다.
