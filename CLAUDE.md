# CLAUDE.md — harness

> 하네스 **자체를 개발할 때** 읽는 맥락 문서.
> 하네스가 무엇이고 어떻게 작동하는지는 [docs/spec.md](docs/spec.md) 가 정본이다.
> 작업을 시작하기 전에 `docs/spec.md` 를 먼저 읽는다. `docs/progress.md` 가 있으면 함께 읽는다.

---

## 프로젝트 정체

| 항목 | 값 |
|---|---|
| 이름 | harness |
| 용도 | Claude Code 기반 개인 개발 하네스 |
| 소유자 | 김규태 |
| 경로 | `C:\Users\kiimw\Desktop\tools\harness` |
| GitHub | [Kimprodp/harness](https://github.com/Kimprodp/harness) (공개) |
| 라이선스 | MIT |
| 배포 방식 | 대상 프로젝트에 `.claude/` 복사. 설치 스크립트가 처리 |

---

## 문서 지도

| 문서 | 내용 | 언제 고치나 |
|---|---|---|
| [README.md](README.md) | 설치와 사용 방법. 하네스를 쓰는 사람용 | 사용 절차나 커맨드 목록이 바뀔 때 |
| [docs/spec.md](docs/spec.md) | 구조 정본. 프로세스, 문서 경계, 각 요소의 역할, 설계 원칙 | **구조를 바꾸기 전에 먼저** |
| `docs/progress.md` | 진행 이력, 작업 계획, 결정 로그 | 작업 단계를 마칠 때마다 |
| 이 파일 | 하네스를 개발할 때의 맥락과 규칙 | 프로젝트 정체나 작업 규칙이 바뀔 때 |

현재 구현 상태와 완료 이력은 이 파일에 두지 않는다. `docs/progress.md` 가 갖는다.
구조 설명과 커맨드 목록도 이 파일에 두지 않는다. `docs/spec.md` 와 `README.md` 가 갖는다.

**`docs/progress.md` 는 git 추적에서 제외돼 있다.** 작업 계획과 판단 과정이 담기는 문서라 저장소에 올리지 않는다. 소유자의 로컬에만 존재하므로 클론한 환경에는 없다. 없으면 `docs/spec.md` 와 git 로그로 현재 상태를 파악한다.

---

## 배경

### 출발점

Claude Code 를 프로젝트마다 처음부터 세팅하는 대신, 반복되는 절차를 한 폴더에 담아 어디에나 떨어뜨릴 수 있게 만든 것이 시작이다. 자동 매뉴얼(훅), 작업 기억(문서), 자동 품질 검사, 전문 에이전트라는 네 갈래를 참고했다.

Phase 4 에서 개인 프로젝트 전용이던 구조를 회사 제품 개발까지 받도록 넓혔다. 시작점이 새 아이디어 인터뷰 하나뿐이었고, 브랜치와 CI 와 마이그레이션이 흐름에 없었고, 시스템 전체를 설계하는 자리가 비어 있었다.

### 코드 레퍼런스

**[gstack](https://github.com/garrytan/gstack)** (MIT)

| 원본 | 하네스 |
|---|---|
| `office-hours` | `idea-validation` |
| `plan-ceo-review` | `scope-review` (4 모드 차용) |
| `plan-eng-review` | `tech-stack-decision` (축약) + `tech-spec` (전체) |

**[diet103/claude-code-infrastructure-showcase](https://github.com/diet103/claude-code-infrastructure-showcase)** (MIT)

- plan / context / tasks 문서 시스템의 원형
- 훅 자동 활성화 구조

---

## 작업 규칙

### 구조를 바꿀 때

1. **`docs/spec.md` 를 먼저 고친다.** 구현이 그 뒤를 따라온다. 반대 순서로 하면 정본이 뒤처진다.
2. 작업 단계를 마치면 `docs/progress.md` 의 Phase 표를 갱신한다.
3. 사용 절차가 바뀌면 `README.md` 도 함께 고친다.
4. 같은 내용을 두 문서에 적지 않는다. 한 곳에 두고 나머지는 링크한다.

### 커맨드와 스킬을 고칠 때

- 커맨드마다 반복되는 공통 규칙(말투, 승인 절차, 에이전트 호출 방식)은 `.claude/templates/CLAUDE.md.template` 의 공통 작업 규칙에 둔다. 커맨드 문서에는 그 커맨드만의 규칙만 남긴다.
- 커맨드는 실행할 때마다 전체가 읽힌다. 실패 시나리오 표나 예시가 길어지면 분리를 검토한다.
- 새 커맨드를 만들기 전에 기존 커맨드의 모드로 흡수할 수 있는지 본다.

### 브랜치와 커밋

- `main` 에 직접 커밋하지 않는다. 작업 브랜치에서 작업하고 PR 로 머지한다.
- 커밋은 작업 단계 단위로 나눈다. 무엇을 왜 바꿨는지가 메시지에 남게 쓴다.
- 푸시는 사용자가 요청할 때만 한다.

### 토론

- 구조적 결정은 사용자와 먼저 상의한다.
- 문서를 고치기 전에 어디를 무엇으로 바꿀지 제안하고 확인받는다.
- 단순 오류 수정은 즉시 반영해도 된다.

---

## 문체

하네스가 만드는 문서와 하네스 자신의 문서에 모두 적용된다.

- 직설적이고 간결하게. "delve", "crucial", "robust" 같은 표현과 "흥미롭네요", "여러 방법이 있죠" 같은 회피형 표현을 쓰지 않는다.
- 구어 표현을 기술 문서 문체로 쓴다. "떨궈 넣으면" 이 아니라 "설치하면".
- 강조는 아껴 쓴다. 라벨마다 굵게를 붙이지 않는다.
- 항목을 숫자로 묶어 이름 붙이지 않는다. 나열하면 읽는 쪽이 센다.
- 문서를 고칠 때 이력 문구를 본문에 남기지 않는다. 처음부터 그렇게 쓴 것처럼 다시 쓴다.

설계 원칙은 [docs/spec.md](docs/spec.md) 의 설계 원칙 절에 있다.
